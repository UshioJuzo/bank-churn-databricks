# Same palette as the report and the notebooks, so the three read as one project.
[theme]
base = "dark"
primaryColor = "#a8471f"
backgroundColor = "#0d1117"
secondaryBackgroundColor = "#161b22"
textColor = "#e6e1d7"
font = "sans serif"

[server]
headless = true
